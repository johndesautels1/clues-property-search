{
  "name": "clues-quantum-property-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "dev": "cd app && npm run dev"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  }
}
