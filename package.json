{
  "name": "clues-quantum-property-dashboard",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "dev": "cd app && npm run dev"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  }
}
